import React from 'react'; 
import Routes from './routes'
import './global.css'

// JSX (JavaScript : XML)

function App() {
  // const [counter, setCounter] = useState(0);

  // useState() retorno uma array [campo que recebe o valor, function para atualizar o valor]

  // function increment () { 
  //   setCounter(counter + 1)
  //  }

  return (
    <Routes></Routes>
  )
} 

export default App;
